server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: any unmatched path serves index.html so React Router
    # (client-side routing) can handle it.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
